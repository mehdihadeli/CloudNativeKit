name: Build, test, and publish

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build-test:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore source projects
        run: dotnet restore CloudNativeKit.slnx

      - name: Build
        run: dotnet build CloudNativeKit.slnx --configuration Release --no-restore -p:PublicRelease=true

      - name: Run unit tests
        run: dotnet test --solution CloudNativeKit.UnitTests.sln --configuration Release --no-build --no-restore -p:SkipVersion=true -p:RunAnalyzers=false

      - name: Run integration tests
        run: dotnet test --solution CloudNativeKit.IntegrationTests.sln --configuration Release --no-build --no-restore -p:SkipVersion=true -p:RunAnalyzers=false

  calculate-version:
    name: Calculate release version
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    needs: build-test
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Install nbgv CLI
        run: dotnet tool install --tool-path "$RUNNER_TEMP/nbgv" nbgv --version 3.10.94

      - name: Calculate version
        id: version
        shell: bash
        run: |
          version="$($RUNNER_TEMP/nbgv/nbgv get-version -v SemVer2)"
          echo "Calculated SemVer2 version: $version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

  publish:
    name: Publish packages and release assets
    if: startsWith(github.ref, 'refs/tags/v') || (github.ref == 'refs/heads/main' && contains(needs['calculate-version'].outputs.version, '-preview.'))
    needs:
      - build-test
      - calculate-version
    runs-on: ubuntu-latest
    environment: nuget
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore source projects
        run: |
          for project in src/*/*.csproj; do
            dotnet restore "$project"
          done

      - name: Pack CloudNativeKit packages
        run: |
          mkdir -p artifacts
          for project in src/*/*.csproj; do
            dotnet pack "$project" --configuration Release --no-restore --output artifacts -p:PublicRelease=true -p:PackageVersion="${{ needs['calculate-version'].outputs.version }}"
          done

      - name: Publish to NuGet.org
        run: dotnet nuget push "artifacts/*.nupkg" --api-key "${{ secrets.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Update matching Release Drafter draft
        if: always()
        uses: release-drafter/release-drafter@v7
        with:
          config-name: release-drafter.yml
          version: ${{ needs['calculate-version'].outputs.version }}
          name: v${{ needs['calculate-version'].outputs.version }}
          tag: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || format('v{0}', needs['calculate-version'].outputs.version) }}
          publish: ${{ startsWith(github.ref, 'refs/tags/v') && !contains(needs['calculate-version'].outputs.version, '-') }}
          prerelease: ${{ contains(needs['calculate-version'].outputs.version, '-') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
